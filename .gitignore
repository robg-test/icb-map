# Compiled binaries (root-anchored: `ukmap` is also the package name)
/ukmap
/ukmap-*

# Generated by tools/build_mesh.py -- rebuild with `make mesh`
assets/uk.mesh
assets/uk.land
assets/uk.sea
assets/regions.tsv

# Licensed NHS artwork, supplied by whoever is entitled to use it (see badge.odin)
assets/logo.png

# WebAssembly output
build/

# Terraform: state can contain resource detail, and .terraform/ is a provider cache
infra/terraform/.terraform/
infra/terraform/*.tfstate
infra/terraform/*.tfstate.*
infra/terraform/terraform.tfvars

# Screenshots are scratch, except the two that are checked in deliberately
*.png
!docs/preview.png
!web/og.png

__pycache__/
.playwright-mcp/
.DS_Store
*.swp
